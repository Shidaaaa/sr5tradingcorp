{
  "name": "sr5-trading-corporation",
  "version": "1.0.0",
  "description": "E-Commerce Website with Inventory, Sales and Booking Management for SR-5 Trading Corporation",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && node index.js",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "seed": "cd server && node seed.js",
    "test:installment": "cd server && npm run test:installment"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
